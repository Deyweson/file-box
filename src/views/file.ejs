<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    teste
  </title>
  <style>
    .file-container {
      display: flex;
    }

    .file-icon>div {
      background-color: rgb(239, 239, 239);
      width: 150px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      border-radius: 20px;
    }

    .file-icon>div>div {
      width: 100px;
      height: 100px;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 10px;

      background-image: url('./image.png');
      background-size: cover;
    }

    .file-icon p {
      text-align: center;
      overflow-y: hidden;
      width: 100px;
      height: 50px;
    }
  </style>
</head>

<body>

  <div class="file-container">
    <a class="file-icon">
      <div>
        <div>
          <%= file.name.split('.')[1] %>
        </div>
        <p>
          <%= file.name.split('.')[0] %>
        </p>
      </div>
    </a>
  </div>

  <button>
    <% const download = `/download/${file.id}` %>
    <a href=<%= download %>>baixar</a>
  </button>
  <button>
    <% const deletePath = `/delete/${file.id}` %>
    <a href=<%= deletePath %>>apagar</a>
  </button>
</body>

</html>